<!-- ```
UseQuery/
├── index.ts
├── hooks.ts
├── queries.ts
``` -->

## Required packages:

"@tanstack/react-query"

## Requires

<QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
